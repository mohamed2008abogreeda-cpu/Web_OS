import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // OpenNext Cloudflare configurations (empty default is fully valid and auto-generated under the hood)
});
